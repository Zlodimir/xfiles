<%= simple_form_for [xfile, note] do |f| %>
	<%= f.input :title %>
	<%= f.input :description %>

	<%= f.input :asset, as: :file, label: "File" %>

	<%= f.button :submit, class: "btn-primary" %>
<% end %>
