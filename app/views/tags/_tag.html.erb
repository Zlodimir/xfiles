<div class="tag">
	<% if policy(note).tag? %>
		<%= link_to "<span></span>".html_safe, remove_note_tag_path(note, tag), method: :delete, remote: true, class: "remove", title: "remove" %>
	<% end %>
	<%= link_to tag.name, search_xfile_notes_path(@note.xfile, search: "tag:#{tag.name}") %>
</div>